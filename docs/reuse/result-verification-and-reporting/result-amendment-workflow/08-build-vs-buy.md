REFERENCE (FHIR DiagnosticReport amendment status pattern) + ENGINE +
ADAPTER (shared OPA for authorization) + BUILD (the amendment workflow,
versioning, and re-notification logic itself — Core Domain). No
independent product exists for this Feature outside a whole LIMS,
already not adopted wholesale.
