# Feature: Result Amendment Workflow

**Module:** Result Verification and Reporting (**Core Domain**).
Correcting/amending a previously-finalized result — the "Result
Amendments" source capability. Directly maps to FHIR `DiagnosticReport`'s
`amended`/`corrected`/`appended` status values.
