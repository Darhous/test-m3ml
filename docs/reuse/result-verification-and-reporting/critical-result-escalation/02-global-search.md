No new search — this Feature's delivery mechanism need (reliable,
multi-channel, tracked notification) is already fully solved by Novu
(Module 5), now its 3rd confirmed reuse. What is platform-specific is the
critical-value threshold rule set and escalation/acknowledgment workflow
itself, which is Core Domain, not an infrastructure search target.
