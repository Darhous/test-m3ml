Reference pattern only: verified result data (from
`result-review-verification-workflow`) is rendered into a
`DiagnosticReport.presentedForm` attachment via a template-driven PDF/
document generator, White-Label-themed (organization branding per Wave
1's Confirmed requirement) — a Library-category integration, not a
separately-deployed service.
