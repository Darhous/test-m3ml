**Final Decision: LIBRARY (specific candidate deferred to implementation
time) + BUILD**

No dedicated open-source "clinical report renderer" product exists
distinct from generic document-templating libraries. Selecting a specific
library is correctly deferred to implementation time once the platform's
own frontend/backend stack is fixed at SAD stage — asserting one now
would guess at a dependency not yet evidenced, violating the No-Guessing
Rule. The report's structure/content and White-Label theming is
platform-owned BUILD.
