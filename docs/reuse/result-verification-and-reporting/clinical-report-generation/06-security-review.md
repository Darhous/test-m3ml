Not evaluated — no specific candidate selected. Generic risk category
note: template-injection is a known risk class for document-templating
engines if patient data is interpolated unsafely — a standard secure-
coding concern (Constitution's OWASP-adjacent guidance) at
implementation time, not specific to any one candidate.
