LIBRARY (specific candidate deferred to implementation time, stack-
dependent) + BUILD (the clinical report's own layout/content structure
and White-Label theming logic, which is platform-differentiating). The
report-rendering library itself is a commodity choice — genuinely lower
priority for this program's evidence-gathering than the Core-Domain-
owned Verification workflow (`result-review-verification-workflow`).
