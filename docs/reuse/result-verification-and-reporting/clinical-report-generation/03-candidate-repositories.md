No specific PDF/templating library candidate selected this pass — the
category (generic document-templating engines) is real and mature, but
choosing a specific one (JasperReports vs. Carbone vs. pdfmake vs.
platform's own frontend-stack-native option) depends on the SAD's
eventual frontend/backend stack choice, which is out of this program's
scope. FHIR `DiagnosticReport`'s `presentedForm` attachment pattern is
the relevant Reference data shape, already logged.
