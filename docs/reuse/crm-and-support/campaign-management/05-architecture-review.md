A campaign is a platform-owned definition (target segment, message
sequence, retention trigger) that reads contact/segment data from
Frappe CRM and dispatches through Novu's multi-channel delivery — the
same composition pattern already used for `payment-gateway-abstraction`
(Module 22) and other composed Features, not a new Engine.
