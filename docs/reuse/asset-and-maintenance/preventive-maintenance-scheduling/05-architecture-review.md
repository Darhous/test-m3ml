Atlas CMMS's PM module generates recurring work orders against the Asset
Registry, with downtime tracked as a work-order state — the same Engine
serving 2 Features from a single adoption point, consistent with the
program's Cross-Feature Dependency principle.
