Atlas CMMS's REST API integrates with the platform's own Analyzer entity
(Module 14) for lab-equipment-specific assets, and with
`calibration-management`/`preventive-maintenance-scheduling`/
`spare-parts-tracking` (this Module) as its own native modules — a
single Engine solving all 4 Features in this Module, the program's
clearest Cross-Feature Dependency case in the Business Modules.
