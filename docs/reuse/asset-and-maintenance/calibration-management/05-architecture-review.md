General-asset calibration is a specialized PM work-order type within
Atlas CMMS. Distinct data domain from Module 14's analyzer/QC-run
calibration (which is tied to the Westgard-rules Reference pattern) —
the two Features are related in name but serve different Aggregates and
are correctly not merged.
