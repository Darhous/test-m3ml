# Integration Options — Appointment Scheduling Engine

Cal.com deployed with its own notification features disabled/bypassed;
reminders routed through Novu (Module 5) instead. Booking events feed
Encounter creation (`encounter-tracking`, this Module) via Domain
Events.
