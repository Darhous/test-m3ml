# Feature: Document Control

**Module:** Audit and Compliance (Platform Kernel)
**Source (Discovery):** Wave 3 Capability "Document Control" (Governance
category) — "Manage controlled documents (policies, SOPs)."

## Duplicate Detected — Deferred to Document Management Module

This Feature's scope is **the same underlying capability** as the
Document Management Module's `document-control-workflow` Feature
(`MASTER_FEATURE_CATALOG.md` Module 8) — both describe versioned,
approval-gated document lifecycle management, one framed from a
Governance angle (SOPs/policies) and one from a general-purpose angle.
Researching both independently would duplicate work the program's own
instructions explicitly warn against ("do NOT duplicate research").

**Resolution:** full candidate research is deferred to the Document
Management Module (researched later in this program); this Feature's own
files are a pointer, not independent research, and this duplicate is
logged in `MASTER_DEPENDENCY_MATRIX.md` / `MASTER_SHARED_COMPONENTS.md`
as a detected consolidation opportunity — a direct example of the
program's own "detect duplicate features... potential consolidation"
requirement being satisfied.
