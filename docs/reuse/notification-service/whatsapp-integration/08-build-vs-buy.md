# Build vs. Buy — WhatsApp Integration

**ENGINE + SDK**: Novu's provider abstraction (Engine, already selected)
+ a BSP SDK (Twilio/360dialog/Meta direct — vendor choice deferred to
implementation time, not fabricated here). No case for building a custom
WhatsApp Business API client.
