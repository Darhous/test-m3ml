# Integration Options — WhatsApp Integration

Configured as one more Novu provider; BSP selection (Twilio/360dialog/
Meta direct) is a configuration/commercial decision at implementation
time, not a code-level dependency given Novu's provider abstraction.
