# Global Search Log — Organization / Branch Hierarchy

No new search round — a business-data hierarchy table (Organization has
many Branches) is standard relational modeling, not a category with
dedicated third-party products. Identity-side hierarchy is already solved
(Keycloak Groups, Module 1's `user-group-management`).
