# Build vs. Buy — Organization / Branch Hierarchy

**BUILD** — a platform-specific business-data schema, correctly not a
reuse candidate. No enterprise product exists (or should exist) purely
for "Organization has many Branches" — this is core domain modeling
(Wave 9, Tenant and Organization Management Context), not infrastructure.
