# Upgrade Strategy — Tenant Configuration

Inherits `../feature-flags/13-upgrade-strategy.md` for the Unleash-backed
portion. The platform-owned `tenant_settings` table follows the
platform's own migration/versioning process (Constitution Section 48).
