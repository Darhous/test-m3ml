# Candidate Repositories — Tenant Configuration

| Candidate | Fit |
|---|---|
| Unleash (already selected, `../feature-flags/`) via variant/strategy payloads | Possible — evidence from this program's search was not conclusive on payload-richness for arbitrary per-tenant config objects |
| Flagsmith | Strong — explicitly designed for combined flags + remote configuration |
| Platform-owned tenant-settings table (BUILD) | Fallback if neither flag engine's config model fits (e.g., complex nested White-Label theme objects) |
