openIMIS's Claims module handles claim digitization, submission, and
review workflow against its Contribution/Product model — the platform's
own diagnostic-order results (Module 12, Module 15) map to claim line
items via FHIR `Claim`/`ClaimResponse` resources.
