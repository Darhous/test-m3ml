openIMIS's Product model (a configurable benefit package with pricing
rules) is reused as the corporate-contract-rate structure — a corporate
client's negotiated rate is modeled as a Product variant, feeding
`pricing-price-lists` (Module 21, ERPNext) as the applicable discount
tier at invoice time.
