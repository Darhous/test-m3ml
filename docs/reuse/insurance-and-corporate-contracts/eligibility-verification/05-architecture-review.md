openIMIS's modular architecture (post-2019 rearchitecture) lets adopters
select specific functional modules rather than the whole monolith — its
Eligibility/Enrollment module can verify coverage status at point of
care using FHIR-based APIs, integrating with the platform's own Patient
(Module 9) and Encounter (Module 11) records without absorbing the whole
openIMIS application.
