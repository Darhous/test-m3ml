# Integration Options — Session Management

Same Adapter boundary as `../authentication/09-integration-options.md`.
The platform's Identity and Access Adapter exposes a "revoke user
sessions" operation to the Tenant Administration UI (Tenant and
Organization Management Module) via Keycloak's Admin REST API, itself
gated by Constitution Section 21 as a Sensitive Operation.
