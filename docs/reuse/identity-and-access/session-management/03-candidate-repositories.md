# Candidate Repositories — Session Management

Same 4 as `../authentication/03-candidate-repositories.md`. All 4
implement standard OIDC/OAuth2 token lifecycle (access + refresh token,
configurable expiry, revocation endpoint).
