# Global Search Log — Multi-Factor Authentication

No new search round run — MFA (OTP/TOTP, WebAuthn/FIDO2) is documented,
long-standing, built-in functionality of all 4 candidates evaluated in
`../authentication/02-global-search.md` (Keycloak, Zitadel, Authentik,
Ory). Verified via each product's own official documentation reference
found in the prior search pass rather than re-querying — re-running an
identical landscape search for a sub-capability of an already-researched
Engine would duplicate research the program's own instructions explicitly
warn against (Cross-Feature Dependency Map discipline).
