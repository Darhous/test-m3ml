# Final Decision — Multi-Factor Authentication

## Decision: **ENGINE + ADAPTER** (Keycloak — same Engine as `authentication`)

Single adoption point; no separate MFA product evaluated as a standalone
decision since all 4 candidates in the original pool support it natively
and Keycloak was already selected.
