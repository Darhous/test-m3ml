# Integration Options — Multi-Factor Authentication

Configured via Keycloak Authentication Flows, bound to specific Client
Scopes/Roles so that step-up MFA can be required only for Sensitive
Operations (Result Verification, Payroll dual-control) rather than every
login — same Adapter boundary as `../authentication/
09-integration-options.md`, no additional integration surface needed.
