A per-tenant theme configuration (stored via Module 2's
`tenant-configuration`, Unleash-backed) drives: (1) the platform's own
BUILD frontend's CSS/logo/domain, (2) Keycloak's theme selection per
Realm/Organization, (3) Superset's embedding SDK theme parameters, (4)
Documenso/Alfresco's branding options. One configuration source, several
already-adopted Engines consuming it — a consolidation, not a new
architecture.
