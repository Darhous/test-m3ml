Custom-domain support requires standard TLS-certificate provisioning per
tenant domain (e.g., automated via Let's Encrypt/ACME) — an
implementation-time infrastructure concern, not a program-level product
finding.
