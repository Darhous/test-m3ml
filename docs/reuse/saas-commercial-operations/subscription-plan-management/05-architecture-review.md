Kill Bill's plan-catalog model (XML-defined plans/phases/pricing tiers)
and subscription-lifecycle state machine (trial → active → cancelled,
with proration) directly serve tenant subscription management,
integrated with Module 2's `tenant-provisioning`/`organization-branch-
hierarchy` as the tenant-identity side.
