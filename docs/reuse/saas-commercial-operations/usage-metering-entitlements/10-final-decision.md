**Final Decision: ENGINE + ADAPTER (shared Kill Bill + shared OPA)**

Cross-Feature Dependency on `subscription-plan-management` and Module 1.
Entitlement decisions are gated through OPA using Kill Bill's usage/plan
state as input — composing 2 already-adopted Engines.
