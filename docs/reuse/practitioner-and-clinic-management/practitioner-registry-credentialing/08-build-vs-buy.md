**REFERENCE (FHIR) + BUILD** — same rationale as Module 9's
`patient-registry-mpi`: no whole-system product adopted to preserve Core
Domain ownership; the data-model standard is reused, the Aggregate and
credentialing workflow are platform-owned.
