Practitioner Aggregate modeled against FHIR's `Practitioner`/
`PractitionerRole` shape; credential-expiry tracking reuses the same
notification/reminder pattern as other expiry-driven Business Rules
(Wave 6), not a new subsystem.
