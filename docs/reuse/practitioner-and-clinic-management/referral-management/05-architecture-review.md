Referral modeled as a Domain Event/Command chain (Wave 5 vocabulary),
FHIR ServiceRequest-shaped, routed to the target Practitioner/Facility
via this Module's own directory (`clinic-facility-directory`).
