No dedicated search — this Feature is a presentation/query layer over
the platform's own Domain Events (Wave 5), not a category with
standalone third-party products. FHIR's `Encounter`/`DiagnosticReport`
timeline conventions (already touched via `../patient-registry-mpi/`)
inform the display shape.
