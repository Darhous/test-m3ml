Consumes Module 1 (identity), `patient-history-timeline` (this Module),
and Analytics' embedded-analytics pattern (Module 7) for any
patient-facing dashboards — a composition of already-selected pieces,
not a new architecture.
