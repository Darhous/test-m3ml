# Integration Options — Patient Registry / MPI

Platform's own Patient Aggregate (Wave 9 Context 1), FHIR-Patient-shaped,
stored RLS-isolated (Module 2). If cross-facility duplicate-identity
issues are later measured, OpenCR can be introduced as an external
identity-resolution service the Patient Management Module calls — not a
replacement for the Aggregate itself.
