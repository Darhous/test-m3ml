# Upgrade Strategy — Patient Registry / MPI

FHIR versions deliberately/slowly (R4/R5) with strong backward
compatibility. If OpenCR is later introduced, it integrates alongside
the existing Aggregate (identity-resolution service), not a data-model
migration — a bounded addition, not a rearchitecture.
