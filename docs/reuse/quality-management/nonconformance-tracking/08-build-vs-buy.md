Same finding as `capa-management`/`complaint-management`: no credible
open-source candidate. BUILD, linking directly into Module 14's
execution-tracking Aggregates rather than introducing a separate system.
