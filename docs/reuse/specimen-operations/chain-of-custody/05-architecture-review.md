Each handoff (collection → transport → accessioning → bench) recorded
as a Domain Event referencing the barcode-scanned accession ID
(`barcode-labeling`), feeding the immudb audit trail (Module 3) given
its Sensitive-adjacent chain-of-integrity role.
