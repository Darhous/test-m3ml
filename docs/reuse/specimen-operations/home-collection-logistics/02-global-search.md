No dedicated search — "offline-first mobile field-collection app" is a
well-known architecture pattern (local-first storage + eventual sync,
e.g., via CouchDB/PouchDB-style replication or a custom queue), not
tied to a specific healthcare product. Whether Offline Mode is even
required remains genuinely Open (`open-questions.md` #6) — this Feature
cannot be fully researched past the pattern level until that Open
Question is answered, consistent with the No-Guessing Rule.
