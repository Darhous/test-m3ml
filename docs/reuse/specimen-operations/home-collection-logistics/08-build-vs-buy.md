Cannot be resolved to BUILD/BUY/etc. today. The decision is gated on
`open-questions.md` #6 (is Offline Mode required at all):

- If Offline Mode is NOT required: this Feature collapses to a thin
  online-only extension of `specimen-accessioning` (Module 13) — no new
  Engine/Library needed, BUILD only.
- If Offline Mode IS required: a local-first sync Library (e.g.
  PouchDB/CouchDB-style, or a custom outbox-pattern queue) would need
  genuine candidate research not yet performed.

Per the No-Guessing Rule, no Final Decision is asserted here.
