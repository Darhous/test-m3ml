ENGINE + ADAPTER (composed: ERPNext for PO matching, shared with
`purchase-requisition-order` — + OpenBoxes for the stock increase itself,
shared with Module 18). A deliberate integration decision to avoid a
Detected Duplicate before one is even created, rather than resolving one
after the fact (as Modules 17/18 had to).
