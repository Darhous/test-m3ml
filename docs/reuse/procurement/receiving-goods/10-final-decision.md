**Final Decision: ENGINE + ADAPTER (composed: ERPNext + OpenBoxes)**

ERPNext handles PO/receipt matching (shared with `purchase-requisition-
order`); OpenBoxes handles the actual stock increase (shared with Module
18), by deliberate design — not ERPNext's own competing stock module.
This Feature demonstrates the program's duplicate-avoidance discipline
applied proactively rather than reactively.
