**Final Decision: ENGINE + ADAPTER (shared ERPNext) + integration (Novu)**

Cross-Feature Dependency on `invoicing-engine`. ERPNext's native Dunning
module directly covers this Feature.

**Module 21 summary:** all 3 Features resolve entirely through ERPNext,
now confirmed reused across 4 Modules (Procurement, Supplier Management,
Billing) — the program's broadest single-Engine footprint after
Keycloak.
