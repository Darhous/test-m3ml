ERPNext's Sales Invoice doctype generates invoices from a Delivery
Note/Sales Order chain, with line items that can map to the platform's
own diagnostic-order/procedure records (Module 12) via its REST API —
this is the point where ERPNext's role expands from Procurement/Supplier
(buy-side) to Billing (sell-side), still the same single Engine adoption,
not a new one.
