ZXing decode → OpenBoxes stock-deduction API call. Also usable by
`specimen-accessioning`'s barcode-labeling (Module 13) sharing the same
ZXing decode library for a consistent scanning experience across the
platform.
