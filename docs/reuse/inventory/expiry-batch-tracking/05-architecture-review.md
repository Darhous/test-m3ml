OpenBoxes's lot/expiry data model (lot number, expiry date, FEFO-ordered
picking, expiry-alert thresholds) is used as-is — this is the Feature
that most directly matches OpenBoxes's own design purpose, requiring the
least adaptation of any Feature in this Module.
