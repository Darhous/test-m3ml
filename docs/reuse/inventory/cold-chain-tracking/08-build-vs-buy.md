ENGINE + ADAPTER (shared OpenBoxes, inventory/alert layer) + BUILD
(sensor-telemetry ingestion, per-vendor adapter, reusing Module 4's
`device-protocol-adapters` pattern). No credible dedicated cold-chain
IoT platform exists at the maturity bar this program requires — a
genuine "searched and correctly rejected what was found" outcome, not a
gap in research effort.
