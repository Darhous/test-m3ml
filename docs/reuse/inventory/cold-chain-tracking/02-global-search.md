WebSearch performed this pass ("open source cold chain temperature
monitoring IoT sensor logging healthcare vaccine 2026"). OpenBoxes
(`stock-management`'s decision) already provides cold-chain-relevant
storage-location and alert support at the inventory-record level
(confirmed during that Feature's search). For real-time IoT sensor
temperature telemetry specifically, the search surfaced only personal/
student GitHub projects (e.g., an ESP32-based vaccine-monitoring repo
with no institutional backing, and a similarly small individual project)
— correctly rejected per the program's screening rule against personal/
student/unmaintained projects, not adopted as false positives.
