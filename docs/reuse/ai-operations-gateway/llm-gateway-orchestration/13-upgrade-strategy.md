# Upgrade Strategy — LLM Gateway / Orchestration

Track Portkey's post-2026-open-sourcing release cadence closely (newer
OSS status than most engines in this program). The single-chokepoint
integration boundary means a future LiteLLM fallback migration is bounded
to the Gateway Module, not platform-wide.
