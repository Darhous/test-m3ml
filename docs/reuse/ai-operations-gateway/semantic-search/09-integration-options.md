# Integration Options — Semantic Search

pgvector extension enabled on the platform's existing PostgreSQL
instance; embeddings generated via the Portkey-routed LLM gateway,
stored alongside tenant-scoped data with the same RLS policies already
established (Module 2).
