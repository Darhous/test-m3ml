# Final Decision — Semantic Search

## Decision: **REFERENCE/LIBRARY (pgvector extension on the already-selected PostgreSQL)**

Qdrant explicitly logged as the evidence-based upgrade path if a real
measured scale/filtering ceiling is ever hit (Constitution Section 55) —
not adopted preemptively.
