# Reusable Assets — Semantic Search

| Asset Type | Asset | Note |
|---|---|---|
| Data Model | pgvector's `vector` column type + HNSW index pattern | Directly reusable for any future embedding-storage need across Modules. |
