# Upgrade Strategy — Semantic Search

Track pgvector releases alongside PostgreSQL's own upgrade cadence
(Module 2). If a real scale/filtering ceiling is measured, Qdrant is the
identified migration target — embedding data can be re-exported and
re-indexed, a bounded migration.
