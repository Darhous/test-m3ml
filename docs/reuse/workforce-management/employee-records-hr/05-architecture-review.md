Frappe HR runs on the same Frappe Framework as ERPNext, sharing its
doctype/REST-API conventions — operationally, adopting it alongside
ERPNext is a much lower-friction integration than adopting an unrelated
HRMS product would be, since both share deployment tooling and data-
model idioms.
