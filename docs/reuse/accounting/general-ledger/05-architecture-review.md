ERPNext's General Ledger automatically posts journal entries from every
already-adopted ERPNext module (Sales Invoice, Purchase Order, Payment
Entry, Credit Note) — this Feature requires no new integration work
beyond what Modules 19-23 already established, since it's the same
Engine's native ledger, not a separate system needing reconciliation.
