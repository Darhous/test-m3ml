# Integration Options — ASTM Integration

Same Gateway boundary as `hl7-integration-engine`. ASTM channel-specific
parsing code isolated within the Gateway, output normalized to the same
platform Domain Event shape regardless of source protocol.
