# Candidate Repositories — Device Protocol Adapters

No per-vendor candidate exists generically. Reused toolkit: Apache
Camel's connector library (or Mirth's custom-connector framework if that
engine is chosen) provides the plumbing; each vendor integration is
necessarily platform-owned BUILD on top.
