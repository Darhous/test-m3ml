# Security Review — Device Protocol Adapters

Per-vendor security posture cannot be pre-assessed generically; each
integration requires its own review at implementation time (Constitution
Section 37). No generic finding beyond the base engine's own review.
