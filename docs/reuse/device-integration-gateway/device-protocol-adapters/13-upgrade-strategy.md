# Upgrade Strategy — Device Protocol Adapters

Each adapter versioned independently under the platform's own release
process; a vendor's API change affects only its own adapter, not the
shared Gateway infrastructure.
