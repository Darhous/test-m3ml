# Global Search Log — Device Protocol Adapters

No dedicated search — by definition, "Vendor API" integrations are
per-vendor and cannot be pre-researched generically; Apache Camel's
broad connector library (`camel-http`, `camel-file`, `camel-ftp`, etc.,
Apache-2.0, already found in `../hl7-integration-engine/`) is the
relevant reusable toolkit, not a specific per-vendor product.
