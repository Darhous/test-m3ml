# Build vs. Buy — Device Protocol Adapters

**BUILD** (per-vendor adapters, correctly), on top of the shared base
engine's connector toolkit. No product can be "bought" for a vendor
integration that doesn't exist yet.
