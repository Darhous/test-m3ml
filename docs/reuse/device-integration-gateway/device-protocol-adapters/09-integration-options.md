# Integration Options — Device Protocol Adapters

Each new vendor device integration follows the same Gateway pattern:
adapter → normalize → platform Domain Event → message broker. New vendor
support is additive, not a re-architecture.
