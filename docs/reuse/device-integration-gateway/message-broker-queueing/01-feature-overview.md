# Feature: Message Broker / Queueing

**Module:** Device Integration Gateway. The durable, at-least-once
delivery backbone already referenced by Module 1's `audit-hooks-
integration` and Module 3's `immutable-audit-trail` Features (both flagged
needing durable broker delivery). This Feature makes that choice
concrete rather than leaving it implicit.
