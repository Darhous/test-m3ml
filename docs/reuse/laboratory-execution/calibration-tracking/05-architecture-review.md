Reference pattern: a Calibration Schedule per Analyzer (frequency, last-
performed date, due date), with calibration runs recorded as a
specialized QC-run subtype — directly reuses
`quality-control-tracking`'s run-recording structure rather than
introducing a separate one.
