No dedicated new search — same finding as `quality-control-tracking`:
calibration tracking is bundled inside whole-LIMS products (SENAITE,
OpenELIS Global), not offered as an independent open-source component.
This Feature is structurally analogous to Module 10's
`clinic-facility-directory` (a join/scheduling structure over
already-modeled Aggregates, here: Analyzer × Calibration-Schedule).
