- OpenELIS Global's FHIR `Task`-based worklist state model (received →
  in-progress → completed → verified) — directly informs the platform's
  own Worklist Aggregate state machine.
- SENAITE's bench/analyzer assignment UI pattern (worklist grouped by
  bench, drag-to-assign) — a UI/UX reference, not code reuse (Plone
  templates are not portable to the platform's own frontend stack).
