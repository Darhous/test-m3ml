Same candidates as Module 4: Mirth Connect 4.5.2 (tentative, frozen-OSS
caveat) / Apache Camel (fallback) for message routing, plus platform-
owned per-analyzer BUILD adapters (Module 4's `device-protocol-adapters`
Feature already established that no product exists per-analyzer-vendor
by definition).
