No standalone candidate. SENAITE's QC module (Westgard-rules
implementation over control-lot results, Levey-Jennings charting) is the
most mature reference implementation found — logged as a Reference
source, not a Feature-level product decision, since it is not separable
from the whole SENAITE application (`worklist-management` already
rejected wholesale SENAITE adoption for Core Domain reasons).
