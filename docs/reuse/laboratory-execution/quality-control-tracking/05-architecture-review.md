Reference pattern: control-lot results stored as time-series per
analyte/instrument, evaluated against the Westgard rule set on each new
QC run, with Levey-Jennings chart as the primary visualization. This is
a well-established, implementation-agnostic statistical-QC architecture,
not tied to any single product.
