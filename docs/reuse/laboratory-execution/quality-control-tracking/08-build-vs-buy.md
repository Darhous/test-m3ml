Same reasoning as `worklist-management`: the only credible products
(SENAITE, OpenELIS Global) embedding this capability were already
evaluated and not adopted wholesale for Core Domain reasons. No
standalone QC-engine product exists to Buy at the Feature level.
REFERENCE (Westgard methodology + SENAITE's module design) + BUILD is
the only evidence-based answer.
