BUILD (per-gateway adapters, no single product covers all needed
providers, same reasoning as Module 4's `device-protocol-adapters`) +
REFERENCE (Omnipay's adapter-pattern architecture) + Vendor decision
(Fawry/Paymob as commercial gateway providers, a business/procurement
choice outside this program's OSS-reuse scope). No open-source product
to Buy for the abstraction layer itself.
