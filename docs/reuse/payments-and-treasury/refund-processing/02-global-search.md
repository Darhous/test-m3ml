No new search — this Feature composes 2 already-decided capabilities:
`payment-gateway-abstraction`'s per-gateway adapters (which must expose
a refund API call per provider) and ERPNext's Credit Note / Payment
Entry reversal (confirmed as part of its standard Accounts Receivable
flow).
