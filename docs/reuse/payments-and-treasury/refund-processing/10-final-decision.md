**Final Decision: BUILD (thin composition) + ENGINE + ADAPTER (shared
`payment-gateway-abstraction` + ERPNext)**

**Module 22 summary:** `payment-gateway-abstraction` is the only Feature
in the Module requiring genuine new research, resolving to BUILD
(per-gateway adapters, no product exists per-provider by definition) +
REFERENCE (Omnipay's architecture pattern); the other 2 Features compose
already-decided capabilities.
